<Window x:Class="ProjectGenetics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectGenetics"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Width="100" Height="35" Click="Button_Click" HorizontalAlignment="Left" Margin="15,0,0,0" FontSize="14pt" Content="Start"/>

        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
        <Label Content="Length(%):  " FontSize="14pt"/>
         <Label x:Name="LLen" FontSize="14pt"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Right">
            <Label Content="Generation:  " FontSize="14pt"/>
            <Label x:Name="LGen" FontSize="14pt" />
        </StackPanel>

        <Canvas x:Name="scene"/>

    </Grid>
</Window>
